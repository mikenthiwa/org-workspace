import { CenterItemDirective } from './center-item.directive';

describe('CenterItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CenterItemDirective();
    expect(directive).toBeTruthy();
  });
});
